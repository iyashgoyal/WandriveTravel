// CommonJS version of package data for Vercel serverless functions
const packageData = [
  // This would contain your package data, but since the api/index.js 
  // already includes the sample packages array, we can just export an empty array
  // or reference the existing array
];

module.exports = {
  packageData
};
